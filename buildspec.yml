version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies...
      - npm install

  build:
    commands:
      - echo "Build started on $(date)"
      - echo "Listing current directory contents:"
      - ls -al
      - echo "Running app.js..."
      - node app.js
